# Browser → localhost:3000/api/* → Next rewrite → Nest :8080
NEXT_PUBLIC_API_URL=/api

# Socket.io connects directly to Nest (keep CORS enabled on backend)
NEXT_PUBLIC_WS_URL=http://localhost:8080

# Server-side rewrite target (next.config.ts)
BACKEND_URL=http://localhost:8080
